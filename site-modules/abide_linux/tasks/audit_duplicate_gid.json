{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Finds and returns duplicate GIDs in /etc/group",
  "implementations": [
    {"name": "audit_duplicate_gid.rb", "requirements": ["shell"]}
  ]
}
