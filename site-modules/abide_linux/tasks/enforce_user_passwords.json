{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Checks for users with blank passwords in /etc/shadow and sets the passwords to the default",
  "parameters": {
    "default_password": {
      "description": "A default password to set for accounts found to have no password",
      "type": "String[1]",
      "sensitive": true
    }
  }
}
